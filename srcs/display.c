#include "ft_select.h"

void choose_style(t_data *data)
{
	if (elem->pick == 1)
	{

	}
}

void display_all(t_data *data)
{
	t_elem		*list;

	exec_tcap("cl");
	exec_tcap("ve");
	list = data->elem;
	// while (list)
	// {
	// 	if (elem->pick == 1)
	// 	{
	//
	// 	}
	// 	if ()
	// 	exec_tcap("me");
	}
	// data->max_column = get_max_column(data);
}
